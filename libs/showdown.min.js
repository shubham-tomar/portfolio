/**
 * Showdown.js Markdown Library Placeholder
 * 
 * This is a placeholder file to prevent errors when the actual Showdown library is not available.
 * For production use, replace this with the actual Showdown library from https://showdownjs.com/
 */

// Create a minimal Showdown-like interface to prevent errors
window.showdown = {
  Converter: function(options) {
    return {
      makeHtml: function(text) {
        // Very basic markdown conversion for placeholder functionality
        const html = text
          .replace(/^# (.*$)/gm, '<h1>$1</h1>')
          .replace(/^## (.*$)/gm, '<h2>$1</h2>')
          .replace(/^### (.*$)/gm, '<h3>$1</h3>')
          .replace(/^#### (.*$)/gm, '<h4>$1</h4>')
          .replace(/^##### (.*$)/gm, '<h5>$1</h5>')
          .replace(/^###### (.*$)/gm, '<h6>$1</h6>')
          .replace(/^\> (.*$)/gm, '<blockquote>$1</blockquote>')
          .replace(/\*\*(.*)\*\*/gm, '<strong>$1</strong>')
          .replace(/\*(.*)\*/gm, '<em>$1</em>')
          .replace(/!\[(.*?)\]\((.*?)\)/gm, '<img alt="$1" src="$2" />')
          .replace(/\[(.*?)\]\((.*?)\)/gm, '<a href="$2">$1</a>')
          .replace(/\n/gm, '<br />');
        
        return '<div class="markdown-content">' + html + 
          '<div class="markdown-notice">(Placeholder markdown renderer active)</div></div>';
      }
    };
  },
  setFlavor: function() {},
  setOption: function() {}
};

console.log('Placeholder Showdown markdown library loaded');
